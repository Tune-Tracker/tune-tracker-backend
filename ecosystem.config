module.exports = {
  apps: [
    {
      name: "python-server", // 애플리케이션 이름
      script: "uvicorn", // 실행할 스크립트
      args: "main:app --host 0.0.0.0 --port 8000 --proxy-headers", // uvicorn 인자
      interpreter: "./.venv/bin/python", // 가상환경의 Python 경로
      watch: false, // 파일 변경 감지 비활성화
      autorestart: true, // 비정상 종료 시 자동 재시작
      max_memory_restart: "500M", // 메모리 초과 시 재시작
      env: {
        NODE_ENV: "development", // 개발 환경 변수
      },
      env_production: {
        NODE_ENV: "production", // 프로덕션 환경 변수
      },
    },
  ],
};
